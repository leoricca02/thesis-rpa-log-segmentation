[pytest]
# Modules live in src/; tests import them by plain module name
# (e.g. `import data_pipeline`), exactly as before the repository
# was reorganised. No source file was modified to enable this.
pythonpath = src
testpaths = tests
addopts = -v
